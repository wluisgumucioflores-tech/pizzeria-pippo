# Copiar a .env (gitignored) para desarrollo local.
PORT=8787
PIPPO_BACKEND_URL=http://localhost:3333

# No hay API key propia de mcp-saas acá: cada cliente MCP manda la suya
# (generada self-serve en Settings → MCP) en el header Authorization de
# cada request, no vive configurada en este proceso.
